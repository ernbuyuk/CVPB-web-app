# Production environment variables
# This app works standalone with localStorage (no backend required)
# But if you want to connect to a backend API, set VITE_API_BASE_URL
VITE_API_BASE_URL=http://localhost:5005
